# Connect IQ build output (regenerated by monkeyc)
/bin/

# Activity files — may contain real GPS tracks; never commit personal ride data.
# Use tools/ + the SDK sample FIT for testing instead.
*.fit
*.FIT

# Crash dumps / OS cruft
*.stackdump
Thumbs.db
.DS_Store

# Developer key must live OUTSIDE the repo (see README "Build & sideload")
*.der
developer_key
